# printscan 종합 검토 (2026-08-08)

코드(edge/cloud) · Zebra/ZPL 기능 · i18n/용어 · 기획을 면밀히 검토한 결과. 병렬 리뷰 3축 + ZPL 도메인 분석 종합.

---

## A. 코드 — edge (온디바이스) 우선순위 결함

| # | Sev | 위치 | 문제 | 수정 |
|---|---|---|---|---|
| E1 | P0 | CloudSyncClient (poll→ack) | ack 실패 시 잡이 재전달 → **라벨 중복 인쇄/중복 소비** | 인쇄된 jobId 로컬 기록 후 재인쇄 스킵(멱등) |
| E2 | P0 | InventoryService `@EventListener onMoved` | 이벤트가 **DB 트랜잭션 내 동기 실행** + 아웃바운드 HTTP → 허브 지연 시 인쇄 경로 정지 | `@TransactionalEventListener(AFTER_COMMIT)`+`@Async`, 타임아웃 |
| E3 | P0 | InventoryService.move/consume | `Product.quantity` **락 없음** → 동시 OUT lost-update | `@Version` 낙관락 또는 원자 `UPDATE ... quantity=quantity+?` |
| E4 | P1 | CloudSyncClient RestClient | **연결/읽기 타임아웃 없음** → 스케줄러 스레드 무한 블록(폴링 전체 정지) | connect 2s/read 5s + 스케줄러 풀 ≥2 |
| E5 | P1 | CloudSyncClient register | 부팅 시 허브 불가면 **영구 미등록**(재시도 없음) | token==null 시 폴링/하트비트에서 백오프 재등록 |
| E6 | P1 | application.properties | **H2 콘솔 노출**(LAN 전체가 DB 임의 SQL) + DB 비번 공란 | 배포 프로필 `h2.console=false`, 비번 설정 |
| E7 | P1 | LabelApiController print-batch | `count`/`copies` **상한 없음** → 100만 장 루프 가능 | ≤1000 검증, 음수 거부, 400 |
| E8 | P2 | LabelService.consume | 모든 변수값을 제품코드로 간주 → **오출고** | 명시적 productCode 변수 지정 |
| E9 | P2 | LabelApiController | 잘못된 elementsJson → 500(“프린터 오류”) | `@ControllerAdvice` IllegalArgument→400, 치수 검증 |
| E10 | P2 | LabelService.printBatch | 중간 실패 시 앞부분 물리 인쇄됐는데 소비 0(드리프트) + 재큐 시 재인쇄 | 라벨별 소비 + 대량 전 프린터 프리플라이트 |
| E11 | P3 | LabelRasterizer QR/바코드 | QR `MARGIN 0`(콰이엇존 없음)·Code128 강제 스케일 → **스캔 실패 위험** | QR margin≥2, 바코드 모듈폭 정수화 |
| E12 | P3 | 테스트 | 렌더/인코더만 커버, inventory/job/consume/controller **무테스트** | 상태머신·소비·검증 단위테스트 |

## B. 코드 — cloud (허브) 우선순위 결함

| # | Sev | 위치 | 문제 | 수정 |
|---|---|---|---|---|
| C1 | P0 | FleetService admin 조회 전부 | **org 스코프 전무** → SaaS에서 타 테넌트 데이터 전량 노출 | 모든 read를 `ByOrgId`로, ConsumptionLog에 orgId 추가 |
| C2 | P0 | AdminApiController print | `devices/{id}/print` 소유 org 미검증 → **타 테넌트 프린터로 인쇄 주입** | device.orgId==callerOrg 검증 |
| C3 | P0 | AdminAuthFilter/HubProperties | 전 테넌트 **단일 admin-token** → 진짜 테넌트 인증 아님 | org 세션/org별 토큰→orgId 매핑 |
| C4 | P1 | FleetService.pollNext/ack | SENT 후 디바이스 죽으면 **잡 영구 SENT**(재큐 없음) + QUEUED→SENT **락 없음**(이중 디스패치) | SENT 리퍼(타임아웃 재큐) + `@Version`/원자 claim |
| C5 | P1 | FleetService.ack | 상태 전이 가드 없음 → 재ack로 `printCount` 이중 증가·DONE 덮어씀 | SENT일 때만 전이(멱등) + 원자 증가 |
| C6 | P1 | Device 직렬화 | `deviceToken`(유일 자격증명)이 대시보드/admin API로 **노출** | DTO/`@JsonIgnore` |
| C7 | P1 | FleetService.consumption | 매 3초 폴링마다 `findAll()`+in-JVM 그룹핑(무한 증가) | SQL `GROUP BY ... SUM` + org/기간 스코프 |
| C8 | P2 | FleetService.heartbeat | 스냅샷 check-then-insert 레이스 → 유니크 위반 | 원자 upsert/재시도 |
| C9 | P2 | application.properties | `ddl-auto=update`+H2 file+콘솔+고정 `ORG-DEMO-KEY`(로그인키 공개) | SaaS=Postgres+Flyway·콘솔off·키생성 |
| C10 | P2 | /register, device 엔드포인트 | 레이트리밋·페이로드 상한 없음 → Device 무한생성/스팸 | 레이트리밋 + 크기 제한 |
| C11 | P3 | 테스트 | 무테스트(상태머신·격리·upsert) | 상태전이/격리/필터 테스트 |

## C. Zebra / ZPL 기능 검토 (도메인)

현재: 전체 라벨 **^GFA(비압축 ASCII hex)** + `^PW/^LL` + `^PQ` + transport(cups/network/rawdev) + ZXing QR/Code128 래스터. → 한글/WYSIWYG 강점. 아래가 개선/추가 여지.

1. **인쇄 농도/속도(`~SD`,`^PR`)**: 미설정=프린터 기본. 열전사 농도는 스캔품질 직결 → 설정 노출. **[P1]**
2. **미디어 캘리브레이션(`~JC`/`^JU`)**: 라벨 길이/갭 자동측정 → **치수 추정/잘림 문제 근본 해결**. "캘리브레이션" 액션 추가. **[P1]**
3. **프린터 상태질의(`~HQES`/`~HS`)**: 용지없음·헤드열림·리본없음·일시정지 감지 → 관측성+오류 시 인쇄 보류. network 9100 양방향 소켓으로 읽기 가능(USB는 제한). **[P1]**
4. **^GFA 압축(Z64/ACS·반복코드)**: 현재 40×25@203=8000B→hex 16000자/장. 배치·네트워크 시 5~10x 절감. **[P2]**
5. **QR 콰이엇존·바코드 모듈폭**(E11): 실스캐너 신뢰성. **[P1]**
6. **회전(90/180/270)**: 마운트 방향 대응(이미지 회전). **[P3]**
7. **DataMatrix(`^BX`)/GS1-128**: 공급망·제약·리테일 규격(ZXing DataMatrix 지원). **[P2]**
8. **커터/필/티어(`^MM`)**: 커터 장착 시 모드. **[P3]**
9. **디더링(Floyd–Steinberg)**: 이미지 요소 추가 시 그레이 표현(텍스트/코드는 임계값이 정답). **[P3]**
10. **폰트 번들링**: 호스트 Noto CJK 의존 → TTF 리소스 번들+font-path 로 재현성 확보(어플라이언스). **[P2]**
11. **미디어 상/좌 오프셋(`^LH`,`^LT`,`^LS`)**: 정렬 미세조정(잘림/치우침 근본 해결). **[P1]**
12. **인쇄 확정 피드백**: CUPS 접수≠물리 인쇄. 9100+`~HS`로 확인. **[P2]**

## D. i18n / 용어 (핵심 요구 — 외국인 비개발자 대상)

- **현황**: 전 UI 하드코딩 한국어, i18n 인프라 전무. 문자열이 (A)Thymeleaf HTML + (B)정적 JS(토스트·동적 테이블) 두 곳.
- **자곤 노출**(운영자가 이해 불가): `mode: cups`, `elementsJson`, `DPI`, `H2 콘솔`, `서버 래스터`, `HID`, 원시 `IN/OUT`·`QUEUED/SENT/DONE`, `잡/큐잉/플릿/디바이스/업싱크/폴링/하트비트`, `{{키}}/접두어/자리수`.
- **아키텍처(무의존)**: `messages_ko/en.properties`+`ResourceBundleMessageSource`+`CookieLocaleResolver`+`LocaleChangeInterceptor`(?lang=en). JS는 `/api/i18n/{lang}.json`+`t()` 헬퍼(키 화이트리스트). **서버 에러도 코드화**해 원시 텍스트 노출 금지.
- **로케일**: ko(기본)+en 우선. 이후 EPS 외국인력 감안 **vi/km/ne/id** 순, 핵심 액션은 픽토그램 병행.
- **용어 사전**: REVIEW의 글로서리 채택(프린터 연결/해상도/입고·출고/인쇄 작업/대기·전송·완료·실패/작업자/장비/라벨 양식/인쇄 미리보기 …).

## E. 기획 / 로드맵 (재정렬)

- **파일럿 블로커(먼저)**: E1·E2·E4·E7(중복인쇄/트랜잭션·타임아웃/상한), C1·C2·C3(SaaS 테넌트 격리·인증) — SaaS 노출 전 필수. 온프렘 파일럿은 격리 리스크 낮음.
- **엔터프라이즈 20%**: 안정성(systemd 실적용)·복원력(잡 리퍼·재시도·프린터 상태)·관측성(상태화면·알림)·i18n.
- **제품 확장**: SaaS=Postgres+Flyway+per-org 인증+페어링UI+과금(소비로그 계량), 규격=GS1/DataMatrix, 운영=Pi 이미지 zero-touch.
- **우선순위 제안**: ①i18n(ko/en)+de-jargon ②edge 복원력(E1/E2/E4/E5/E7) ③Zebra 농도/캘리브레이션/상태/콰이엇존 ④cloud 격리·상태머신(C1~C6) ⑤테스트 ⑥Postgres/GS1/압축/알림.

---

> 이 문서 이후 구현 순서: **i18n+de-jargon → edge 복원력/Zebra 품질 → cloud 격리/상태머신 → 테스트**. 각 웨이브 빌드/실기계 검증 후 커밋.
