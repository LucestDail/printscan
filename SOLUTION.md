# printscan — 솔루션 정의 (제품 관점)

> 확정 2026-08-07. 구현 아키텍처는 `PLAN-V2.md`, 진행 로그는 커밋 히스토리 참조.
> 한 줄: **공장 생산라인마다 "라즈베리파이 + Zebra 프린터" 유닛을 두고, 라벨을 디자인·인쇄하고, 무엇을 얼마나 인쇄/소비했는지 온프렘 또는 클라우드 허브로 통합 관리하는 어플라이언스.**

---

## 1. 물리 구성 (라인 유닛)
- **Zebra 프린터**(예: ZD421 203dpi) + **3D 프린팅 케이스** 안에 **라즈베리파이**.
- 연결: **Zebra(USB-B) → Pi(USB-A, host)**. Pi 리눅스가 `/dev/usb/lp0`(CUPS raw 큐)로 직접 제어. → `.25`에서 검증한 구성과 동일.
- Pi = **edge 앱** 상주(단일 Spring Boot, 로컬 UI + 래스터 렌더 + 로컬 인쇄 + 허브 동기화).
- 공장 **유선 네트워크**에 연결(별도망 가능).

## 2. 허브 (관리 프로그램) — 하나의 모듈, 두 배포
동일한 `cloud/`(허브) 모듈을 배포만 다르게:
- **(A) 온프렘 MAIN**: 공장장(캡틴) PC에 설치. 공장 LAN 폐쇄망. 인터넷 불필요.
- **(B) SaaS Cloud (AWS)**: 인터넷. 각 프린터/디바이스 고유번호로 접근. 멀티공장·멀티테넌트.
- Pi(edge)는 `printscan.cloud.base-url`만 허브 주소로 바꾸면 됨(온프렘 IP or SaaS 도메인).

## 3. 통신 & 보안 (환경별 차등)
- **통신**: 디바이스가 **아웃바운드 HTTP 폴링**으로 허브에 연결(방화벽/NAT 친화). 인쇄지시 = 허브 잡 큐 → 디바이스 폴링 수령 → 로컬 인쇄 → ack.
- **보안 (환경 맞춤 — TLS를 무조건 적용하지 않음)**:
  - **온프렘 LAN**: 폐쇄망이라 공인 TLS 불필요. **디바이스 토큰**만으로 경량 인증. 허브 admin은 LAN 신뢰.
  - **SaaS(AWS)**: 인터넷 노출 → **TLS(리버스 프록시) + admin 토큰 인증 + 테넌트 격리** 적용. `printscan.hub.admin-token` 설정 시 활성(미설정=온프렘 오픈).

## 4. 핵심 기능
### 4-1. 라벨 (완료)
- 서버 래스터(^GFA): 한글/QR/바코드/박스, 미리보기=인쇄물(WYSIWYG). 캔버스 드래그 디자이너. 템플릿 CRUD.

### 4-2. 연속/배치 출력 = **일련번호 자동증가**
- 템플릿의 `{{seq}}`(또는 지정 변수)를 **prefix + 시작번호 + 자리수 패딩**으로 N장 연속 증가 출력.
- 예: prefix `NET-`, start 1, count 100, pad 4 → `NET-0001` … `NET-0100`. 배치 단위로 이력 남김.

### 4-3. 소비 추적 = **인쇄 = 자동 출고기록**
- 라벨이 **제품 코드**를 담으면(예: `{{code}}`가 등록 제품), 인쇄 시 그 제품 재고를 **인쇄 매수만큼 자동 출고(OUT)** 하고 이력에 **라인 + 작업자 + 시각** 기록.
- 허브 대시보드에서 **라인별 / 작업자별 소비(출력) 집계** 확인 → "누가 뭘 얼마나 가져갔나".
- 디바이스 = 라인(예: `printscan.line.name=1라인`), 인쇄 요청에 작업자(operator) 선택/입력.

## 5. 운영/배포
- **Pi 이미지화**: systemd 서비스로 edge 자동 상주(부팅 시 기동, 크래시 재시작). `deploy/`에 유닛/설치 스크립트.
- **온프렘 허브**: 공장장 PC에 jar + systemd(또는 Docker).
- **SaaS 허브**: AWS(EC2/ECS) + TLS 프록시.

## 6. 지금까지(2026-08-07) vs 남은 것
- ✅ 라벨 엔진·디자이너·스캔재고·허브·네트워크출력 실기계 실증(P0~P4, main e488315).
- ⏭️ 이 문서 기준 추가: 라인/작업자 소비추적, 일련번호 배치, 허브 듀얼배포/보안 차등, Pi systemd 패키징.
