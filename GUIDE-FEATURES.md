# printscan 기능별 상세 가이드

> 각 기능을 **무엇을 · 어디서 · 어떻게** 순으로 설명한다. 기술 레퍼런스는 [`SPEC.md`](SPEC.md), 개요는 [`README.md`](README.md).
> 화면: **edge**(온디바이스, `http://<PI_IP>:8091`) — 홈 `/` · 디자이너 `/designer` · 스캔 `/scan`. **cloud**(허브, `http://<HUB_IP>:8092`) — 대시보드 `/`.

## 목차
1. [라벨 디자이너](#1-라벨-디자이너)
2. [인쇄 — 단일 / 일련번호 배치](#2-인쇄--단일--일련번호-배치)
3. [라벨 실측(눈금자)·캘리브레이션](#3-라벨-실측눈금자캘리브레이션)
4. [스캔 & 재고 입출고](#4-스캔--재고-입출고)
5. [소비 추적 (인쇄 = 자동 출고)](#5-소비-추적-인쇄--자동-출고)
6. [네트워크(원격) 출력](#6-네트워크원격-출력)
7. [중앙 템플릿](#7-중앙-템플릿)
8. [플릿 관리·집계 대시보드](#8-플릿-관리집계-대시보드)
9. [org-key 로테이션](#9-org-key-로테이션)
10. [다국어(언어 변경)](#10-다국어언어-변경)
11. [프린터 상태·헬스·관측성](#11-프린터-상태헬스관측성)

---

## 1. 라벨 디자이너
**무엇** — 한글/QR/바코드/박스를 배치해 라벨 양식을 만든다. **미리보기 = 실제 인쇄물**(서버 래스터).
**어디서** — edge `/designer`.
**어떻게**
1. 라벨 크기(mm)·해상도(dpi, 기본 203) 설정. *치수를 모르면 §3 눈금자로 실측.*
2. 요소 추가 — 종류: **TEXT**(한글 가능)·**QR**·**DATAMATRIX**·**BARCODE**(Code128, 옵션 GS1-128)·**BOX**. 캔버스에서 드래그로 위치/크기 조절(좌표·크기는 mm 기준).
3. **변수 바인딩**: 텍스트/코드 값에 `{{name}}`, `{{code}}` 처럼 쓰면 인쇄 시 입력값으로 치환. → 같은 양식으로 여러 제품 인쇄.
4. 오른쪽 미리보기(PNG)로 확인 — 보이는 그대로 인쇄된다.
5. **템플릿 저장** → 이후 목록에서 불러 재사용/수정/삭제.

**팁** — QR은 우측 배치 + 여백(콰이엇존) 확보. 한글이 안 보이면 §11 폰트 확인.

## 2. 인쇄 — 단일 / 일련번호 배치
**무엇** — 디자인한 라벨을 실제 출력. 매수 지정, 또는 일련번호 자동증가 배치.
**어디서** — edge `/designer`(인쇄 버튼) 또는 API `/api/labels/print`·`/print-batch`.
**어떻게**
- **단일/다매**: 변수값 입력 → 매수(copies, 1~1000) → 인쇄.
- **일련번호 배치**: 접두사(prefix)·시작번호(start)·자리수(pad)·개수(count) 지정 → `NET-0001 … NET-0100` 자동 연속 출력. 라벨마다 이력이 남아 중간 실패해도 어긋나지 않는다.

## 3. 라벨 실측(눈금자)·캘리브레이션
**무엇** — 자 없이 실제 인쇄 가능폭을 측정하고, 미디어 정렬을 교정.
**어떻게**
- **눈금자 인쇄**: `POST /api/labels/ruler/print?widthMm=60` → 라벨에서 **잘리는 지점 = 실제 인쇄 가능폭**. 그 값으로 디자이너 크기 확정.
- **캘리브레이션**: `POST /api/labels/calibrate`(~JC) → 프린터가 라벨 길이/갭을 재측정(치우침/잘림 교정).
- **농도/속도**: 스캔 품질이 낮으면 `PRINTSCAN_PRINTER_DARKNESS`(0~30)·`_SPEED` 조정.

## 4. 스캔 & 재고 입출고
**무엇** — 바코드/QR를 스캔해 제품을 조회하고 입고·출고·조정.
**어디서** — edge `/scan`.
**어떻게**
1. 바코드 스캐너로 코드 스캔(HID 키보드 방식: 숨은 입력창 포커스 + Enter 확정) → 제품 자동 조회.
2. 미등록 코드면 **제품 등록** 유도(코드·이름·단위·최소재고). *코드/이름은 필수, 재고는 음수 불가.*
3. **입고(IN)/출고(OUT)/조정(ADJUST)** — 수량·작업자·비고 입력. 재고 부족 출고는 거부된다.
4. **이력** 조회로 최근 변동 확인(누가/언제/얼마).

## 5. 소비 추적 (인쇄 = 자동 출고)
**무엇** — 라벨에 **제품 코드**가 있으면, 인쇄가 곧 그 제품의 **자동 출고**로 기록된다. "누가 뭘 얼마나 가져갔나"를 집계.
**어떻게**
1. 제품을 미리 등록(§4).
2. 라벨 양식에 `{{code}}` 요소로 제품 코드를 넣는다.
3. 인쇄 시 작업자(operator) 지정 → 인쇄 매수만큼 재고 **OUT** + **라인·작업자·시각** 기록.
4. 허브 대시보드에서 **라인별/작업자별/제품별 소비 집계** 확인(§8).
> 유닛(Pi)의 라인 이름은 `PRINTSCAN_LINE_NAME`으로 설정(예: 1라인).

## 6. 네트워크(원격) 출력
**무엇** — 허브에서 특정 라인 유닛에 **원격으로 인쇄 지시**. 현장에 가지 않고 중앙에서 출력.
**어디서** — cloud 대시보드 `/`(네트워크 출력 카드).
**어떻게**
1. 대상 장비 선택 → 라벨 정의(크기·요소·변수·매수, 일련번호 옵션) 입력 → **이 장비로 인쇄**.
2. 허브가 잡을 큐에 넣고(`QUEUED`), 대상 유닛이 폴링으로 받아(`SENT`) 로컬 인쇄 → 완료 보고(`DONE`/`FAILED`).
3. 잡 목록에서 상태 확인. 유닛이 잠시 꺼져 있어도, 켜지면 자동 수령. 재전달 시 중복 인쇄되지 않는다(멱등).

## 7. 중앙 템플릿
**무엇** — 조직 공통 라벨 양식을 허브에서 관리 → 모든 유닛이 자동으로 받아 사용.
**어디서** — cloud 대시보드(중앙 템플릿 카드).
**어떻게** — 허브에서 템플릿 추가/삭제 → 각 유닛(edge)이 주기적으로 폴링해 로컬에 동기화. 현장마다 따로 만들 필요 없음.

## 8. 플릿 관리·집계 대시보드
**무엇** — 전체 라인 유닛의 상태와 생산/소비 현황을 한눈에.
**어디서** — cloud 대시보드 `/`.
**보이는 것** — 장비 수·온라인 수·누적 인쇄·대기 잡, 장비별 온라인/인쇄수/최근 접속, **라인별/작업자별/제품별 소비 집계**, 재고 스냅샷, 최근 잡.

## 9. org-key 로테이션
**무엇** — 조직(테넌트)을 식별하는 org-key를 **무중단으로 교체**하거나, 유출 시 즉시 폐기.
**어디서** — cloud 대시보드(조직 키 카드) 또는 API `/api/admin/org/*`.
**어떻게**
- **재발급**: 유예(분) 지정 → 새 키 발급. 유예 동안은 **기존 키도 유효**하므로 유닛을 순차 갱신(무중단).
- **직전 키 즉시 폐기**: 키 유출 시 유예 없이 이전 키 무효화.
- 각 유닛은 `provision-edge.sh --org-key <새키>`로 갱신([`deploy/README-PI.md`](deploy/README-PI.md)).

## 10. 다국어(언어 변경)
**무엇** — UI·오류 메시지를 **한국어/영어/베트남어/인도네시아어**로. 외국인 작업자 대응.
**어떻게** — 화면 우측 상단 언어 스위처(KO·EN·VI·ID) 또는 URL `?lang=en`. 서버 오류 메시지까지 선택 언어로 나온다.

## 11. 프린터 상태·헬스·관측성
- **프린터 상태**(용지없음/헤드열림): `GET /api/printer/status` — **network(9100) 모드만** 지원(USB/CUPS는 단방향).
- **헬스**: `GET /actuator/health` — `printer`·`cloud`·`db`·`diskSpace` 구성요소.
- **메트릭**: `GET /actuator/prometheus` — 인쇄/잡/소비 카운터(Grafana). [`deploy/OBSERVABILITY.md`](deploy/OBSERVABILITY.md).
- **알림**: 저재고·프린터 미발견·백업 실패·오프라인을 webhook으로(설정 시).
